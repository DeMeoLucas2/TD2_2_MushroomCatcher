<UserControl x:Name="MenuControle" x:Class="MushroomCatcher.Ecran_affichage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="600" Width="800">

    <Grid x:Name="Ecran_affichage1">

        <Grid.Background>
            <ImageBrush ImageSource="/Image/Menu.png"/>
        </Grid.Background>

        <Button x:Name="ButJouer" HorizontalAlignment="Left" Margin="90,500,0,0" VerticalAlignment="Top" Background="#00000000" Click="ButJouer_Click" Width="180" Height="51" BorderBrush="{x:Null}">
            <Image Source="/Image/Jouer.png"/>
        </Button>

        <Button x:Name="ButCredit" HorizontalAlignment="Left" Margin="300,500,0,0" VerticalAlignment="Top" Background="{x:Null}" Click="ButCredit_Click" Width="180" Height="51" BorderBrush="{x:Null}">
            <Image Source="/Image/Credit.png"/>
        </Button>

        <Button x:Name="ButQuitter" HorizontalAlignment="Left" Margin="550,500,0,0" VerticalAlignment="Top" Background="Transparent" Click="ButQuitter_Click" Width="180" Height="51" BorderBrush="{x:Null}">
            <Image Source="/Image/Quitter.png"/>

        </Button>

    </Grid>

</UserControl>